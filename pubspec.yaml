name: vishwaspay
version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.12.0
  firebase_auth: ^5.1.0
  cloud_firestore: ^4.4.0
  url_launcher: ^6.0.20
  http: ^0.14.0
  provider: ^6.1.3
  shared_preferences: ^3.1.4
  image_picker: ^1.0.0
  google_sign_in: ^5.5.0
  firebase_storage: ^11.3.0
  flutter_local_notifications: ^13.0.3
  dio: ^5.5.0

flutter:
  uses-material-design: true
